pipeline{
    agent any
    stages{
        stage("build"){
            steps{
                // npm i 
                // npm build
                echo "========building the Application========"
            }
        }
        stage("test") {
            steps{
                // npm test
                echo "========tesing the Application========"
            }
        }
        stage("deploy") {
            steps{
                echo "========deploying the Application========"
            } 
        }
    }
    
}
